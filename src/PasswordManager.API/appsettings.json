{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "JwtSettings": {
    "SecrectKey": "${{ secrets.JWT_SECRECT_KEY }}",
    "Issuer": "PasswordManager",
    "Audience": "PasswordManager",
    "ExpiryMinutes": 60
  }
}
